{
  "name": "@loopstorm/supervisor",
  "version": "1.1.0",
  "description": "LoopStorm Guard AI Supervisor — runtime risk analysis and anomaly detection",
  "license": "MIT",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "start": "bun run src/index.ts",
    "test": "bun test",
    "lint": "biome check src/ tests/",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@trpc/client": "^11.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "*",
    "@loopstorm/api": "*",
    "@types/bun": "latest",
    "typescript": "*"
  }
}
